---
paths:
  - "src/**/*.tsx"
---

# React

- Ensure root-level elements of components for large menus/panels/headers are identified with id property even if it is not used for styling of dom queries. It is needed for LLM context and ease of inspection via browser dev tools.
